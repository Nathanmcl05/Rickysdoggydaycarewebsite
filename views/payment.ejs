<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Payment | Ricky’s Doggy Daycare</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<!-- Icons -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
  :root {
    --blue: #1e88e5;
    --yellow: #fdd835;
    --white: #ffffff;
    --light-blue: #e3f2fd;
    --dark-blue: #1565c0;
    --gray: #f5f5f5;
    --text-dark: #333;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, Helvetica, sans-serif;
  }

  body {
    background-color: var(--light-blue);
    color: var(--text-dark);
  }

  header {
    background: linear-gradient(90deg, var(--blue), var(--yellow));
    color: var(--white);
    text-align: center;
    padding: 40px 20px;
  }

  header i {
    font-size: 3rem;
    margin-bottom: 10px;
  }

  nav {
    background-color: var(--blue);
    display: flex;
    justify-content: center;
    gap: 30px;
    padding: 15px;
  }

  nav a {
    color: var(--white);
    text-decoration: none;
    font-weight: bold;
  }

  main {
    max-width: 450px;
    margin: 40px auto;
    padding: 0 20px;
  }

  .payment-container {
    background-color: var(--white);
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  }

  .payment-container h2 {
    text-align: center;
    color: var(--dark-blue);
    margin-bottom: 25px;
  }

  .payment-form {
    display: grid;
    gap: 15px;
  }

  .payment-form label {
    font-weight: bold;
  }

  .payment-form input,
  .payment-form select {
    padding: 10px;
    border-radius: 6px;
    border: 1px solid #ccc;
    font-size: 1rem;
  }

  .payment-form button {
    padding: 14px;
    background-color: var(--blue);
    color: var(--white);
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    cursor: pointer;
    transition: 0.2s;
  }

  .payment-form button:hover {
    background-color: var(--dark-blue);
  }

  .note {
    font-size: 0.9rem;
    color: #555;
    text-align: center;
    margin-top: 10px;
  }

  footer {
    background-color: var(--blue);
    color: var(--white);
    text-align: center;
    padding: 20px;
    margin-top: 40px;
  }
</style>
</head>

<body>

<header>
  <i class="fa-solid fa-credit-card fa-2x"></i>
  <h1>Payment</h1>
  <p>Complete your booking securely 🐾</p>
</header>

<nav>
  <a href="index.html"><i class="fa-solid fa-house"></i>Home</a>
  <a href="booking.html"><i class="fa-solid fa-calendar-check"></i>Booking</a>
</nav>

<main>
  <div class="payment-container">
    <h2>Payment Details</h2>

    <form class="payment-form">
      <label for="name">Full Name</label>
      <input type="text" id="name" placeholder="John Doe" required>

      <label for="email">Email</label>
      <input type="email" id="email" placeholder="example@email.com" required>

      <label for="card">Card Number</label>
      <input type="text" id="card" placeholder="1234 5678 9012 3456" required>

      <label for="expiry">Expiry Date</label>
      <input type="month" id="expiry" required>

      <label for="cvc">CVC</label>
      <input type="text" id="cvc" placeholder="123" required>

      <button type="submit"><i class="fa-solid fa-paw"></i> Pay Now</button>

      <p class="note">This is a demo page. No real payments are processed.</p>
    </form>
  </div>
</main>

<footer>
  <p>© 2026 Rick's Doggy Daycare 🐕</p>
</footer>

<script>
  const form = document.querySelector('.payment-form');
  form.addEventListener('submit', function(e) {
    e.preventDefault();
    alert('Payment completed successfully!');
    form.reset();
    // Optionally redirect back to home or confirmation page
    // window.location.href = 'index.html';
  });
</script>

</body>
</html>
